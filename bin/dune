(menhir
 (modules parser))

(ocamllex lexer)

(executable
 (public_name stat_lang)
 (name main)
 (libraries stat_lang)
 )

; ((library
;  (name stat_lang)
;  (public_name stat_lang)
;  (libraries stat_lang)))
